Review all tests created in intermediate TDD steps; determine which are still necessary to retain.
Review all unused methods and classes and remove them where appropriate.
The order of all retrieved alternative flights must remain the same as before, with the previous implementation.
Verify that the errors thrown before remain the same now, and that no side effects changed except the intentional Redis storage and retrieval structure change.
